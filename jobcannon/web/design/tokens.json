{
  "$vendored": "From swole (private, github.com/Senkichi/swole) docs/design/living-journal/tokens.json @ 163da4fd9c1d42dc57fe125e0165da9cec680ca8 (2026-07-30). Re-sync: docs/design/living-journal.md. Do not hand-edit values.",
  "$schema": "https://design-tokens.github.io/community-group/format/",
  "$description": "Living Journal design system — 'the paper training journal's organization rendered in living ink'. Paper surfaces hold still; only the ink moves, and every stroke is dead straight. Authored 2026-07-18 for swole; reusable across projects.",
  "color": {
    "$description": "Two contrast tiers per accent, per WCAG 2.1. The base token (inkSecondary/semantic) clears 3:1 against both page and card and is correct for large text (≥18px, or ≥14px at font-weight ≥700), icons, strokes, borders, and other graphical objects (non-text contrast, WCAG 1.4.11). For text below that size/weight threshold, use the paired '-text' token (inkSecondaryText/semanticText) instead, which clears 4.5:1 (WCAG 1.4.3, body text) against both page and card. A base token used directly on body text is the defect this pairing exists to fix — do not reintroduce it. Dark-theme '-text' values alias their base token unchanged, since dark already clears 4.5:1 with one value; the pairing exists in dark too only so a consumer can always reach for the '-text' variant without a per-theme branch.",
    "light": {
      "$description": "LIGHT · PAPER — warm cream page, near-black ink.",
      "page": { "$type": "color", "$value": "#FAF6EF", "$description": "App background; warm paper." },
      "card": { "$type": "color", "$value": "#FFFDF8", "$description": "Raised paper surface; one cream step above page." },
      "ink": { "$type": "color", "$value": "#1E1611", "$description": "Primary text and drawn marks. Warm dark brown — mirrors the dark theme's page background, so light and dark reflect one another." },
      "inkSecondary": { "$type": "color", "$value": "#7A7A85", "$description": "Secondary text, equipment gray in figures. 3:1 tier — large text/icons/graphics only; for body text use inkSecondaryText (see color.$description)." },
      "inkSecondaryText": { "$type": "color", "$value": "#70707A", "$description": "Body-text-safe variant of inkSecondary. 4.5:1 on page (4.546:1) and card (4.818:1)." },
      "hairline": { "$type": "color", "$value": "#1E161124", "$description": "Card borders and rules: ink at ~14% alpha." },
      "rule": { "$type": "color", "$value": "#EAE1D2", "$description": "Soft ledger rules on paper." },
      "semantic": { "$type": "color", "$value": "#2F9E44", "$description": "STRICTLY semantic green: done marks, PR/progression, form-safety (spine), signature accent rule. Never decorative. 3:1 tier — large text/icons/graphics only; for body text use semanticText (see color.$description). semanticText inherits the same STRICTLY-semantic constraint; it does not relax it." },
      "semanticText": { "$type": "color", "$value": "#1F7A40", "$description": "Body-text-safe variant of semantic, for the rare case semantic green marks body-size text (e.g. an inline status word). 4.5:1 on page (4.978:1) and card (5.275:1). Same STRICTLY semantic usage rule as semantic." }
    },
    "dark": {
      "$description": "DARK · LAMPLIT PAPER — the journal by lamplight: deep warm espresso, cream ink. Never blue-black.",
      "page": { "$type": "color", "$value": "#1E1611", "$description": "App background; deep espresso paper." },
      "card": { "$type": "color", "$value": "#2A2018", "$description": "Raised warm surface." },
      "raised": { "$type": "color", "$value": "#33271C", "$description": "Second elevation step (chips, stamp fills)." },
      "ink": { "$type": "color", "$value": "#EDE5D8", "$description": "Primary text: warm cream/bone." },
      "inkSecondary": { "$type": "color", "$value": "#A89A88", "$description": "Secondary text: warm taupe, never cool gray. Already clears 4.5:1 (6.488:1 page / 5.798:1 card)." },
      "inkSecondaryText": { "$type": "color", "$value": "#A89A88", "$description": "Aliases inkSecondary unchanged — dark already clears 4.5:1, so no separate value is needed. Kept as a distinct token so a consumer always reaches for '-text' for body text regardless of theme." },
      "hairline": { "$type": "color", "$value": "#EDE5D824", "$description": "Bone at ~14% alpha." },
      "equipment": { "$type": "color", "$value": "#9C8F80", "$description": "Figure equipment stroke in dark." },
      "dialTrack": { "$type": "color", "$value": "#3A2D20", "$description": "Empty track of rest dial / progress strokes." },
      "semantic": { "$type": "color", "$value": "#3DBD55", "$description": "Semantic green brightened one step for AA (~6.5:1) on card brown. Already clears 4.5:1 (7.307:1 page / 6.530:1 card)." },
      "semanticText": { "$type": "color", "$value": "#3DBD55", "$description": "Aliases semantic unchanged — dark already clears 4.5:1. Same STRICTLY semantic usage rule as semantic; kept as a distinct token for API uniformity with light." }
    }
  },
  "typography": {
    "display": {
      "$description": "Fraunces (OFL) — every numeral that matters (loads, reps, timers) and screen titles. Fallback Georgia, serif.",
      "fontFamily": { "$type": "fontFamily", "$value": ["Fraunces", "Georgia", "serif"] },
      "numeralStyle": { "$type": "string", "$value": "tabular lining figures, optically centered" }
    },
    "body": {
      "$description": "Inter (OFL) — labels, body copy, navigation. Fallback system-ui, sans-serif.",
      "fontFamily": { "$type": "fontFamily", "$value": ["Inter", "system-ui", "sans-serif"] }
    },
    "label": {
      "$description": "Small-caps letterspaced labels (section headers, box headers, nav).",
      "fontFamily": { "$type": "fontFamily", "$value": ["Inter", "system-ui", "sans-serif"] },
      "letterSpacing": { "$type": "dimension", "$value": "0.12em" },
      "textTransform": { "$type": "string", "$value": "uppercase" }
    }
  },
  "stroke": {
    "$description": "Drawn-mark weights, inherited from the line-art animation rig (512px canvas reference).",
    "figureBody": { "$type": "dimension", "$value": "4px", "$description": "Ink body strokes in exercise figures, round caps/joins." },
    "cardHairline": { "$type": "dimension", "$value": "1.5px", "$description": "Card borders at hairline alpha." },
    "accentRule": { "$type": "dimension", "$value": "4px", "$description": "The signature straight accent rule (e.g., under a screen title). One per screen." }
  },
  "radius": {
    "card": { "$type": "dimension", "$value": "16px" },
    "tab": { "$type": "dimension", "$value": "10px", "$description": "Week-strip journal tabs — all seven identical." },
    "button": { "$type": "dimension", "$value": "14px" },
    "stepper": { "$type": "dimension", "$value": "50%", "$description": "Stepper rings are circles." }
  },
  "motion": {
    "$description": "Ink moves like a spring — and ink is always straight. Paper surfaces hold still; only drawn marks animate. Clean geometry only: straight rules and perfect arcs, no wobble, no irregular paths.",
    "spring": { "$type": "cubicBezier", "$value": [0.34, 1.56, 0.64, 1], "$description": "Press/settle curve for steppers and small controls (overshoot-and-settle)." },
    "drawOn": { "$type": "duration", "$value": "600ms", "$description": "Stroke-draw reveals (accent rule, set-check) via dash-offset; ease-out." },
    "drain": { "$type": "string", "$value": "linear, wall-clock anchored", "$description": "Rest dial/bar ink drains in real time on a perfect arc or straight bar." },
    "breathe": { "$type": "duration", "$value": "2400ms", "$description": "Soft scale pulse (≤2%) for the single primary CTA when idle; subtle." },
    "principles": {
      "$type": "string",
      "$value": "1) Paper never bounces — surfaces do not translate/scale on interaction. 2) Ink does the moving: draws itself on, drains, checks in. 3) Every animated mark is straight or a perfect arc. 4) One breathing element per screen maximum. 5) Semantic green may move; decoration may not exist."
    },
    "reducedMotion": {
      "$type": "string",
      "$description": "Resolution contract for prefers-reduced-motion (web) / a disabled system animator scale (Android, ANIMATOR_DURATION_SCALE=0, read via rememberInkMotionEnabled()). Applies to every current and future animated token.",
      "$value": "Governing principle: no element's meaning may depend on having seen it animate — any new animated token must declare its static end state at definition time. Under reduced motion, every animated token resolves to that end state immediately, with no transition: drawOn (accent rule, set-check) -> renders fully drawn, not mid-stroke or unstarted. Count-up numerals -> render at final value (no count-up token or animation exists in this codebase yet; when one is added, it must honor this rule from the start, not retrofit it). DrawnCheck -> renders checked, i.e. the same fully-drawn end state as drawOn. breathe -> holds still at 1x scale, no pulse. An animation that is merely shortened in duration does not satisfy this if it is also set to repeat indefinitely — an infinite-iteration animation must stop iterating, not just iterate faster; and an animation without a forwards fill must actually rest at its intended end value once stopped, not snap back to its start."
    }
  },
  "iconography": {
    "$description": "Line-art in figure stroke style: round caps, ink color, no fills. The 35 exercise Lottie animations are first-class brand elements — give them framed 'form panel' homes, never thumbnails.",
    "navStyle": { "$type": "string", "$value": "outlined line icons, small-caps labels, active = ink-filled + small dot beneath" }
  }
}
