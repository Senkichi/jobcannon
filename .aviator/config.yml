merge_rules:
  labels:
    trigger: "mergequeue"
  preconditions:
    number_of_approvals: 0
    required_checks:
      - "Tests passed"
      - "CLA Assistant Lite"
  merge_strategy:
    name: "squash"
